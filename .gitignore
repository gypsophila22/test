# 의존성 관리 폴더 (Node.js)
node_modules/
# npm에서 생성하는 디버그 로그 파일
npm-debug.log*
# yarn에서 생성하는 디버그 로그 파일
yarn-debug.log*
yarn-error.log*

# 빌드/컴파일 결과물
# 프로젝트를 빌드했을 때 생성되는 결과물 폴더
dist/
build/
out/
# Next.js 빌드 결과물
.next/
# Nuxt.js 빌드 결과물
.nuxt/
# Parcel 번들러 캐시
.parcel-cache/
# 테스트 커버리지 보고서
coverage/

# 민감한 정보 (절대 Git에 올리면 안 됨!)
# 환경 변수 파일 (API 키, 비밀번호 등 민감 정보 포함)
.env
.env.local
.env.development.local
.env.test.local
.env.production.local

# 운영체제 관련 파일
# macOS에서 생성되는 숨김 파일
.DS_Store
# Windows에서 생성되는 썸네일 캐시 파일
Thumbs.db

# IDE 관련 파일 (Visual Studio Code, IntelliJ 등)
# VS Code 설정 폴더 (개인 설정이므로 보통 무시)
.vscode/
# IntelliJ IDEA 설정 폴더
.idea/
# 기타 IDE 관련 파일
*.iml
*.ipr
*.iws

# 임시 파일 및 로그 파일
*.log
*.tmp
temp/